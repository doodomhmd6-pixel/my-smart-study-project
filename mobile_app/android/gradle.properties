org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true

# The following lines are added to explicitly disable any proxy settings
# that might be causing Gradle to fail to connect to the internet.
# This is a common cause for the "NDK not configured" error, as Gradle
# cannot download required files to verify the NDK's status.
#systemProp.http.proxyHost=
#systemProp.http.proxyPort=80
#systemProp.http.proxyUser=
#systemProp.http.proxyPassword=
#systemProp.https.proxyHost=
#systemProp.https.proxyPort=80
#systemProp.https.proxyUser=
#systemProp.https.proxyPassword=
